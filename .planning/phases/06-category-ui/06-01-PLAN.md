---
phase: 06-category-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/VideoFeed.tsx
autonomous: false
requirements:
  - CATUI-01
  - CATUI-02
  - CATUI-03

must_haves:
  truths:
    - "Finance and Tech tabs are visible at the top of the feed on first load"
    - "Tapping a tab switches to that category's feed without a full page reload"
    - "Switching from one tab to the other and back resumes the same video position — it does not reset to video 1"
    - "Each tab shows only its own category's videos — no cross-category mixing"
  artifacts:
    - path: "frontend/components/VideoFeed.tsx"
      provides: "Tab bar UI, per-tab scroll position memory, offset-adjusted overlays"
      contains: "CATEGORY_LABELS"
  key_links:
    - from: "frontend/components/VideoFeed.tsx"
      to: "/api/today?category={next}"
      via: "switchCategory fetch in useCallback"
      pattern: "fetch.*api/today.*category"
    - from: "tab bar button onClick"
      to: "switchCategory"
      via: "onClick handler calling switchCategory(cat)"
      pattern: "switchCategory\\(cat\\)"
    - from: "tabScrollState ref"
      to: "feedRef.current.scrollTop"
      via: "save before switch, restore after data loads"
      pattern: "tabScrollState"
---

<objective>
Complete the category tab bar UI in VideoFeed.tsx so users can switch between Finance and Tech feeds, and per-tab scroll position is preserved when switching back.

Purpose: CATUI-01, CATUI-02, CATUI-03 — the three remaining requirements for the v1.1 Multi-Category milestone.
Output: A fully working tab bar in VideoFeed.tsx with scroll memory.
</objective>

<execution_context>
@/Users/jaimeberdejosanchez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaimeberdejosanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/components/VideoFeed.tsx
@frontend/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tab bar JSX, fix variable shadowing, and adjust overlay offsets</name>
  <files>frontend/components/VideoFeed.tsx</files>
  <action>
The partial work already in VideoFeed.tsx includes: `Category` type, `CATEGORY_LABELS` constant, `category` state, `editionList` state, and `switchCategory` callback. The JSX tab bar, overlay offsets, and one bug still need attention.

**Bug to fix first (line 53):**
Remove `const allEditions = editionList` — it shadows the `allEditions` prop and causes a TypeScript error. All usages of `allEditions` in JSX (lines 181, 183, 235) must be replaced with `editionList` directly.

**Add tab bar JSX:**
Insert a tab bar as the FIRST child inside the outer container `<div>` (before the edition navigation bar). The tab bar must:
- Be positioned `absolute`, `top: 0`, `left: 0`, `right: 0`, `zIndex: 60` (above everything else)
- Have `paddingTop: 'calc(env(safe-area-inset-top) + 8px)'`, `paddingBottom: '8px'`, `paddingLeft: '16px'`, `paddingRight: '16px'`
- Have `background: 'linear-gradient(to bottom, rgba(0,0,0,0.85) 0%, transparent 100%)'`
- Contain a `<div>` with `display: 'flex'`, `gap: '8px'`
- Map over `Object.entries(CATEGORY_LABELS)` to render one button per category
- Each button: `onClick={e => { e.stopPropagation(); switchCategory(cat as Category) }}`
- Active button style (when `cat === category`): white background (`rgba(255,255,255,1)`), black text, `fontWeight: 600`
- Inactive button style: `rgba(255,255,255,0.15)` background, white text, `fontWeight: 400`
- Both buttons: `border: 'none'`, `borderRadius: '20px'`, `padding: '5px 14px'`, `fontSize: '0.82rem'`, `fontFamily: 'system-ui, -apple-system, sans-serif'`, `cursor: 'pointer'`, `color` as above
- Disable buttons while `isLoadingEdition` is true (set `disabled={isLoadingEdition}`)

**Adjust overlay offsets:**
The tab bar is always present (not conditional like the edition nav bar), so all overlay top values must account for the tab bar height (~44px total including safe-area padding).

The tab bar height in practice is: `env(safe-area-inset-top) + 8px (paddingTop) + ~26px (button height) + 8px (paddingBottom)` = approximately `env(safe-area-inset-top) + 42px`. Use `env(safe-area-inset-top) + 50px` as the tab bar bottom clearance constant.

Update the following:

1. **Edition navigation bar** (`paddingTop`): change from `'calc(env(safe-area-inset-top) + 8px)'` to `'calc(env(safe-area-inset-top) + 50px)'`

2. **Progress dots `top`**: was conditional on `hasMultipleEditions`:
   - When `hasMultipleEditions` true: was `calc(env(safe-area-inset-top) + 46px)` — change to `calc(env(safe-area-inset-top) + 96px)`
   - When `hasMultipleEditions` false: was `calc(env(safe-area-inset-top) + 10px)` — change to `calc(env(safe-area-inset-top) + 58px)`

3. **Mute button `top`**: was conditional on `hasMultipleEditions`:
   - When `hasMultipleEditions` true: was `calc(env(safe-area-inset-top) + 40px)` — change to `calc(env(safe-area-inset-top) + 90px)`
   - When `hasMultipleEditions` false: was `calc(env(safe-area-inset-top) + 8px)` — change to `calc(env(safe-area-inset-top) + 52px)`
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/AutoNews_AI/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Run `npm run dev` in frontend/, open http://localhost:3000, confirm two tabs "Finance" and "Tech" are visible at top. Tap "Tech" — feed updates. Tap "Finance" — feed switches back. No TypeScript errors.</manual>
  </verify>
  <done>
    - TypeScript compiles with zero errors
    - Finance and Tech tabs render at the top of the feed
    - Active tab has white background with black text; inactive has translucent background
    - Tapping a tab calls switchCategory and updates currentEdition from /api/today?category=...
    - Variable shadowing bug (const allEditions = editionList) removed
  </done>
</task>

<task type="auto">
  <name>Task 2: Per-tab scroll position memory (CATUI-03)</name>
  <files>frontend/components/VideoFeed.tsx</files>
  <action>
Currently `switchCategory` resets `activeIndex` to 0 and sets `feedRef.current.scrollTop = 0`, which violates CATUI-03 (switching back must resume the same video position). Fix this by storing and restoring per-tab scroll state.

**Add a ref to hold per-tab scroll state** (place after existing refs):
```
const tabScrollState = useRef<Record<Category, { activeIndex: number; scrollTop: number }>>({
  finance: { activeIndex: 0, scrollTop: 0 },
  tech: { activeIndex: 0, scrollTop: 0 },
})
```

**Update `switchCategory`** to save the current tab's scroll state before switching, and restore the new tab's saved state after the data loads. The updated logic:

```
const switchCategory = useCallback(async (next: Category) => {
  if (next === category) return

  // Save current tab's scroll state
  tabScrollState.current[category] = {
    activeIndex,
    scrollTop: feedRef.current?.scrollTop ?? 0,
  }

  setCategory(next)
  setIsLoadingEdition(true)
  setEditionIndex(0)

  // Restore saved scroll state for the next tab
  const saved = tabScrollState.current[next]
  setActiveIndex(saved.activeIndex)
  if (feedRef.current) feedRef.current.scrollTop = saved.scrollTop

  try {
    const res = await fetch(`/api/today?category=${next}`, { cache: 'no-store' })
    const data = await res.json()
    setCurrentEdition(data.edition ?? null)
    setEditionList(data.all_editions ?? [])
    // After edition loads, restore scroll to saved position
    // Use requestAnimationFrame to wait for DOM to update with new videos
    requestAnimationFrame(() => {
      if (feedRef.current) feedRef.current.scrollTop = saved.scrollTop
      setActiveIndex(saved.activeIndex)
    })
  } catch {
    // stay on current
  } finally {
    setIsLoadingEdition(false)
  }
}, [category, activeIndex])
```

Note: `activeIndex` must be added to the `useCallback` deps array (it was not there before). This is correct — we need the current activeIndex value at the time the user switches tabs.

**Edge case:** When a user first visits the tech tab (no prior scroll saved), `saved = { activeIndex: 0, scrollTop: 0 }` which is the correct default (start from the first video).
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/AutoNews_AI/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>
      1. Open http://localhost:3000, scroll to video 2 on Finance tab
      2. Tap "Tech" tab — should load from Tech video 1
      3. Tap "Finance" tab — should resume at video 2 (the position you left)
      4. TypeScript compiles clean.
    </manual>
  </verify>
  <done>
    - tabScrollState ref exists and tracks { activeIndex, scrollTop } per category key
    - switchCategory saves current category scroll state before switching
    - switchCategory restores saved scroll state for the target category after data loads
    - TypeScript compiles with zero errors
    - Switching from Tech back to Finance resumes at the previously viewed video position
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — tab bar UI and scroll memory</name>
  <what-built>
    Tab bar UI with Finance and Tech tabs (Task 1) and per-tab scroll position memory (Task 2). Both auto tasks are complete and TypeScript compiles clean.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` inside frontend/
    2. Open http://localhost:3000
    3. Confirm: Two tabs labeled "Finance" and "Tech" appear at the top of the feed
    4. Confirm: Active tab has white pill background; inactive has translucent background
    5. Tap "Tech" — confirm feed switches to tech videos without page reload
    6. Scroll to video 2 or 3 on the Tech tab
    7. Tap "Finance" — confirm Finance feed appears
    8. Tap "Tech" again — confirm Tech feed resumes at the video you were on (not video 1)
    9. Tabs are disabled (dimmed) while loading — no double-tap issues
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
  <action>Human verification step — no automated action required. Confirm visual tab bar behavior and scroll memory as described in how-to-verify.</action>
  <verify>User confirms all 9 verification steps pass and types "approved".</verify>
  <done>User types "approved" — all CATUI requirements confirmed working in browser.</done>
</task>

</tasks>

<verification>
After both auto tasks:
- `cd frontend && npx tsc --noEmit` returns 0 errors
- Tab bar renders with Finance and Tech pills
- switchCategory fetches /api/today?category={next} and updates the feed
- tabScrollState.current saves/restores per-tab positions
- All existing functionality (edition nav, progress dots, mute button) still works with updated offsets
</verification>

<success_criteria>
- CATUI-01: Finance and Tech tabs visible at top on first load — tab bar JSX renders unconditionally
- CATUI-02: Tapping a tab switches feed without page reload — switchCategory fetches fresh data client-side
- CATUI-03: Switching back to a tab resumes the previous video position — tabScrollState ref saves/restores per-tab state
- Zero TypeScript errors
- All pre-existing UI elements (edition nav bar, progress dots, mute button) correctly offset below the tab bar
</success_criteria>

<output>
After completion, create `.planning/phases/06-category-ui/06-01-SUMMARY.md`
</output>
