---
phase: 08-auth-ui-ios-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01-PLAN.md
  - 08-02-PLAN.md
files_modified:
  - frontend/app/auth/auth-error/page.tsx
  - frontend/app/auth/update-password/page.tsx
autonomous: false
requirements:
  - AUTH-06
  - AUTH-07

must_haves:
  truths:
    - "Failed OAuth exchanges redirect to a visible error page (not a blank screen or 404)"
    - "The auth-error page shows a plain 'Sign-in failed' message and a link back to the feed"
    - "The update-password page renders a form for setting a new password after a reset link is clicked"
    - "Real iPhone with PWA installed to home screen: Google OAuth sign-in via bottom sheet completes and returns user to the feed"
  artifacts:
    - path: "frontend/app/auth/auth-error/page.tsx"
      provides: "OAuth error fallback page"
    - path: "frontend/app/auth/update-password/page.tsx"
      provides: "Password update form for reset flow"
  key_links:
    - "/auth/callback/route.ts already redirects to /auth/auth-error on exchange failure — this page must exist"
    - "updatePassword Server Action in actions.ts already handles the form submission for update-password"
---

<objective>
Create the two missing auth pages (`/auth/auth-error` and `/auth/update-password`) that were referenced in Phase 7 routes but left as Phase 8's responsibility. Then run the real-device iOS PWA validation checkpoint.

Purpose: Without `/auth/auth-error`, a failed OAuth exchange hits a 404. Without `/auth/update-password`, the password reset email link has no destination. The iOS checkpoint gates Phase 9 from starting — if OAuth is broken on real iPhone PWA, the recovery cost is HIGH.

Output: Two auth pages, real-device iOS validation passed.
</objective>

<execution_context>
@/Users/jaimeberdejosanchez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaimeberdejosanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/app/auth/callback/route.ts
@frontend/app/auth/actions.ts
@frontend/app/auth/confirm/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /auth/auth-error page</name>
  <files>frontend/app/auth/auth-error/page.tsx</files>
  <action>
    Create `frontend/app/auth/auth-error/page.tsx` as a minimal Server Component.

    The existing `/auth/callback/route.ts` already redirects here on failed OAuth code exchange (`NextResponse.redirect(...'/auth/auth-error')`). Without this page, users hit a 404 after a failed sign-in.

    Requirements:
    - Server Component (no `'use client'` needed — static content only)
    - Centred layout filling the viewport
    - Dark background (`#000`) matching the app's overall aesthetic
    - Headline: "Sign-in failed" (white, ~1.2rem, semibold)
    - Supporting text: "Something went wrong during sign-in. Please try again." (muted white, ~0.875rem)
    - A link `<a href="/">` styled as a button: "Back to feed" — background `rgba(255,255,255,0.1)`, border `1px solid rgba(255,255,255,0.2)`, white text, rounded, `padding: 10px 24px`, `marginTop: 24px`
    - Safe area padding: `paddingTop: env(safe-area-inset-top)`, `paddingBottom: env(safe-area-inset-bottom)`
    - All inline styles — no CSS classes (consistent with the rest of the app)
    - No external imports other than React (this is a standalone static page)
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/AutoNews_AI/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Navigate to http://localhost:3000/auth/auth-error — confirm the error page renders with "Sign-in failed" message and a "Back to feed" link.</manual>
  </verify>
  <done>frontend/app/auth/auth-error/page.tsx exists, compiles cleanly, renders a non-404 error page with a link back to /.</done>
</task>

<task type="auto">
  <name>Task 2: Create /auth/update-password page</name>
  <files>frontend/app/auth/update-password/page.tsx</files>
  <action>
    Create `frontend/app/auth/update-password/page.tsx` as a `'use client'` component.

    Context: The `resetPassword` Server Action in `actions.ts` sends a magic link with `redirectTo: .../auth/confirm?next=/auth/update-password`. After the user clicks the link, they land here and need to enter a new password. The `updatePassword(formData)` Server Action already exists in `actions.ts` — it calls `supabase.auth.updateUser({ password })` and then `redirect('/')`.

    Implementation:
    - `'use client'` component (needs form state for pending/error UX)
    - Import `updatePassword` from `@/app/auth/actions`
    - State: `pending` (boolean), `error` (string | null)
    - Form with:
      - Password input (`type="password"`, name="password", placeholder="New password", minLength=8)
      - Confirm password input (`type="password"`, name="confirm", placeholder="Confirm new password")
      - Client-side validation before calling Server Action: check passwords match, check length >= 8. Show error inline if validation fails (do not call the Server Action).
      - Submit button: "Update password" — disabled while pending
    - On submit: set `pending = true`, call `await updatePassword(formData)` in a try/catch, set `error` on catch, set `pending = false`.
    - On success: `updatePassword` calls `redirect('/')` server-side — no client redirect needed.

    Styling: Consistent with the app. Dark background (`#000`), centred column layout, white labels, inline styles throughout. Safe area padding. Input style: `background: rgba(255,255,255,0.08)`, `border: 1px solid rgba(255,255,255,0.15)`, `borderRadius: 8px`, `color: white`, `padding: 12px 16px`, `fontSize: 0.95rem`, `width: 100%`. Submit button: `background: white`, `color: #000`, `borderRadius: 8px`, `padding: 12px`, `fontWeight: 600`, `width: 100%`, `cursor: pointer`.

    Max content width: 400px, centred with `margin: 0 auto`, `padding: 32px 24px`.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/AutoNews_AI/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Navigate to http://localhost:3000/auth/update-password — confirm the password form renders with two password fields and a submit button.</manual>
  </verify>
  <done>frontend/app/auth/update-password/page.tsx exists, compiles cleanly, renders a password update form that calls the updatePassword Server Action.</done>
</task>

<task type="checkpoint:human-verify">
  <name>iOS PWA Real-Device Validation</name>
  <files></files>
  <action>
    This is the iOS PWA validation gate. The blocker noted in STATE.md says: "must test on real iPhone with PWA installed before Phase 8 closes; recovery cost is HIGH if auth is broken and social phases are already built on top of it."

    Deploy the current branch to Vercel (or test against the dev tunnel if running locally accessible from iPhone).

    **Test checklist (perform on real iPhone with PWA installed to home screen via Add to Home Screen):**

    1. **Guest browsing (AUTH-06)**
       - Open the PWA from the home screen icon
       - Scroll through all videos
       - Confirm: no sign-in prompts, no banners, videos play normally

    2. **Social action sheet (AUTH-07)**
       - Tap the like button on any video
       - Confirm: bottom sheet slides up from the bottom with "Sign in to like this" headline
       - Confirm: "Continue with Google" button is visible with Google branding
       - Tap the overlay — confirm sheet dismisses, video continues

    3. **Google OAuth flow (iOS PWA — the critical test)**
       - Note the current video index (e.g., video 2 of 5)
       - Tap the like button → sheet opens
       - Tap "Continue with Google"
       - Confirm: page navigates to Google sign-in (NOT a popup — full page navigation)
       - Complete Google sign-in with a test account
       - Confirm: redirected back to the app at the SAME video you were watching
       - Confirm: URL is clean (no ?videoIndex= in the address bar)
       - Confirm: user is signed in (tapping social buttons no longer shows the sheet)

    4. **Session persistence**
       - Close the PWA completely (swipe up from app switcher)
       - Reopen from home screen icon
       - Confirm: user is still signed in (social buttons don't trigger the sheet)

    **Pass criteria:** All 4 tests pass on real device. If any fail, document the failure before closing this plan.
  </action>
  <verify>
    <automated>MISSING — iOS PWA standalone mode cannot be automated. Human verification required.</automated>
    <manual>All 4 test scenarios above pass on real iPhone with PWA installed to home screen.</manual>
  </verify>
  <done>Real-device iOS PWA validation complete. Google OAuth works via window.location.href in standalone mode. Video position is restored on OAuth return. Session persists across PWA close/reopen.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 (both new pages compile cleanly)
- Navigating to `/auth/auth-error` returns 200 with error message and "Back to feed" link (not a 404)
- Navigating to `/auth/update-password` returns 200 with a password update form
- iOS PWA checkpoint: Google OAuth via bottom sheet completes successfully on real iPhone with PWA installed
- Video position is preserved across the OAuth redirect on real device
</verification>

<success_criteria>
1. `/auth/auth-error` page renders without a 404 — the callback route's failure path now has a destination.
2. `/auth/update-password` page renders the password update form and correctly calls the existing `updatePassword` Server Action.
3. iOS PWA real-device test passes: OAuth completes via `window.location.href` (full page navigation, not popup), user returns to the correct video, session persists after PWA close/reopen.
4. Phase 8 blocker in STATE.md can be marked resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-ui-ios-validation/08-03-SUMMARY.md` following the summary template.
</output>
