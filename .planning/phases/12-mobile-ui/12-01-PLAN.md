---
phase: 12-mobile-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/VideoItem.tsx
  - frontend/components/TabBar.tsx
autonomous: true
requirements:
  - MOB-01
  - MOB-02
  - MOB-04

must_haves:
  truths:
    - Video fills 100% of the viewport height — no info panel visible below the video
    - Title and "Leer artículo completo" link are always visible, overlaid at bottom-left with a dark gradient behind them
    - Like, bookmark, and comment buttons appear in a vertical right-rail column overlaid on the video (top-to-bottom order: like → bookmark → comment), each with its count below
    - Social button state (isLiked, isBookmarked, counts) is unchanged — only render position moves
    - TabBar total height is calc(56px + env(safe-area-inset-bottom)); icons and labels are never cut off on iPhone notch devices
    - Bottom overlay text does not overlap the right-rail buttons (right padding clears 72px)
  artifacts:
    - frontend/components/VideoItem.tsx (full-screen layout, right-rail social, bottom overlay)
    - frontend/components/TabBar.tsx (safe-area-correct height)
  key_links:
    - VideoItem receives all existing props unchanged (onSocialAction, likeCount, isLiked, isBookmarked, commentCount)
    - feed-item CSS class still controls snap height — VideoItem root only needs position:relative to anchor overlays
    - bottom overlay paddingBottom: calc(env(safe-area-inset-bottom) + 56px + 16px) clears TabBar + breathing room
---

<objective>
Convert VideoItem from a flex-column layout (video above, info panel below) to a full-screen video with absolutely-positioned overlays matching TikTok's pattern. Move social buttons from the bottom info panel to a right-rail vertical column. Fix TabBar safe-area height so icons are never cut off on iPhone 14+ (home indicator devices).

Purpose: The current layout wastes ~130px of screen on the info panel. Full-screen video is the core v1.3 Mobile UI goal. The TabBar safe-area fix is a prerequisite so the bottom overlay clearing calculation is correct.

Output: VideoItem.tsx (full-screen layout), TabBar.tsx (correct safe-area height). No new files. No changes to VideoFeed, VideoGrid, or any API.
</objective>

<execution_context>
@/Users/jaimeberdejosanchez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaimeberdejosanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-mobile-ui/12-CONTEXT.md
@.planning/phases/12-mobile-ui/12-RESEARCH.md
@frontend/components/VideoItem.tsx
@frontend/components/TabBar.tsx
@frontend/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TabBar safe-area height</name>
  <files>frontend/components/TabBar.tsx</files>
  <action>
The current TabBar sets `height: '56px'` with `paddingBottom: 'env(safe-area-inset-bottom)'`. On iPhone 14+ (safe-area-inset-bottom = 34px), the total element height is 90px but only 56px is allocated — icons get clipped.

Change the outer div height to `calc(56px + env(safe-area-inset-bottom))` while keeping `paddingBottom: 'env(safe-area-inset-bottom)'`. This ensures 56px of tap area above the home indicator at all times.

No other changes. The z-index (200), background (#000), and tab logic are unchanged.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/AutoNews_AI/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm TabBar div style has height: 'calc(56px + env(safe-area-inset-bottom))' and paddingBottom: 'env(safe-area-inset-bottom)'</manual>
  </verify>
  <done>TabBar div height is calc(56px + env(safe-area-inset-bottom)); paddingBottom is env(safe-area-inset-bottom); TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Convert VideoItem to full-screen overlay layout</name>
  <files>frontend/components/VideoItem.tsx</files>
  <action>
Replace the existing flex-column layout (video wrapper div + info panel div) with a full-screen overlay pattern. The root `<div className="feed-item">` stays — the `feed-item` CSS class already sets height:100dvh and scroll-snap. Only the INTERIOR layout changes.

**New structure inside `<div className="feed-item">`:**

1. Video element — `position: absolute; inset: 0; width: 100%; height: 100%; objectFit: cover`. Remove the intermediate wrapper div (the `flex: '1 1 0'` div). Attach `ref={videoRef}` and all existing video attributes (src, muted, playsInline, preload, loop, onEnded) unchanged.

2. Bottom overlay div — absolutely positioned:
   ```
   position: absolute
   bottom: 0
   left: 0
   right: 72px        ← leaves room for right-rail (72px = rail width + margin)
   padding: 16px 16px calc(env(safe-area-inset-bottom) + 56px + 16px) 16px
   background: linear-gradient(to top, rgba(0,0,0,0.75) 0%, transparent 100%)
   zIndex: 10
   ```
   Contains (in order):
   - Date label paragraph (unchanged styles, keep existing conditional)
   - Headline paragraph (unchanged styles, WebkitLineClamp: 2)
   - "Leer artículo completo" link (unchanged styles)

3. Right-rail social column div — absolutely positioned:
   ```
   position: absolute
   right: 12px
   bottom: calc(env(safe-area-inset-bottom) + 56px + 80px)
   display: flex
   flexDirection: column
   alignItems: center
   gap: 24px
   zIndex: 10
   ```
   Contains three button stacks (each is a div with flexDirection:column, alignItems:center, gap:4px):
   - Like button: heart SVG (width=28, height=28) + count span below. Fill color: isLiked ? '#ef4444' : 'none'. Stroke: isLiked ? '#ef4444' : 'rgba(255,255,255,0.9)'. Count: fontSize 0.75rem, color rgba(255,255,255,0.85).
   - Bookmark button: bookmark SVG (width=28, height=28) + no count (bookmarks are private). Fill: isBookmarked ? '#facc15' : 'none'. Stroke: isBookmarked ? '#facc15' : 'rgba(255,255,255,0.9)'.
   - Comment button: chat SVG (width=28, height=28) + count span below. Stroke: rgba(255,255,255,0.9).
   Each button: background:none, border:none, cursor:pointer, padding:4px, color inherited.
   onClick handlers: unchanged — `e.stopPropagation(); onSocialAction?.('like'/'bookmark'/'comment', video.id)`.

**Remove:** The old `flex: '0 0 auto'` info panel div and its interior social action row entirely. The `formatDateTime` function and `dateLabel` variable remain — used in the bottom overlay.

**DO NOT change:** VideoItemProps interface, any prop names, the `feed-item` className on the root div, the video's src/muted/playsInline/preload/loop/onEnded attributes.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/AutoNews_AI/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Visually verify in browser: video fills full screen, title visible at bottom-left with gradient, social buttons visible at bottom-right as vertical column</manual>
  </verify>
  <done>
    - VideoItem renders as position:relative full-screen container with video filling inset:0
    - Bottom overlay sits at bottom-left (right:72px to clear rail) with gradient background
    - Right-rail column has like → bookmark → comment from top to bottom, counts below like and comment
    - TypeScript compiles clean (npx tsc --noEmit exits 0)
    - No changes to VideoItemProps interface
  </done>
</task>

</tasks>

<verification>
Run: `cd /Users/jaimeberdejosanchez/projects/AutoNews_AI/frontend && npx tsc --noEmit`
Expected: 0 errors.

Visual check: Open http://localhost:3000 in browser. Confirm:
1. Video fills viewport — no black info panel below
2. Headline and article link visible overlaid at bottom-left with dark gradient behind
3. Like/Bookmark/Comment icons visible at right edge in vertical column
4. Like count shown below heart icon; comment count below chat icon
5. TabBar visible at bottom; on real device (or devtools iPhone simulation) icons are not cut off
</verification>

<success_criteria>
- Video fills 100dvh with no info panel below
- Title + link overlaid bottom-left, readable against gradient
- Social buttons overlaid bottom-right as vertical rail (like → bookmark → comment)
- All social interactions (like, bookmark, comment tap) still dispatch onSocialAction correctly
- TabBar height is calc(56px + env(safe-area-inset-bottom))
- `npx tsc --noEmit` passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-mobile-ui/12-01-SUMMARY.md` with:
- What was changed in VideoItem.tsx and TabBar.tsx
- Any deviations from the plan
- Current state of TypeScript compilation
</output>
