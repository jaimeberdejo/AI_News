---
phase: 13-demo-auth-simplification
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - frontend/components/AuthBottomSheet.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "Guest user who taps a social action (like/bookmark/comment) sees a 'Create account' link below the Google button in AuthBottomSheet"
    - "Tapping 'Create account' navigates to /auth/login (which now has the Register tab by default)"
    - "Google sign-in button in AuthBottomSheet continues to work as-is"
    - "Supabase email confirmation is disabled so newly registered users land in the app immediately after redirect('/')"
  artifacts:
    - path: "frontend/components/AuthBottomSheet.tsx"
      provides: "Create account link below Google button navigating to /auth/login"
      contains: "/auth/login"
  key_links:
    - from: "frontend/components/AuthBottomSheet.tsx"
      to: "/auth/login"
      via: "window.location.href or router.push"
      pattern: "/auth/login"
---

<objective>
Add a "Create account" link to AuthBottomSheet so guests triggered from the video feed social action gate can navigate to the new registration tab, and confirm the Supabase dashboard email confirmation toggle is disabled for the frictionless demo flow.

Purpose: Close the second registration surface gap — demo users who hit the social action gate are no longer forced to use Google OAuth.
Output: Modified AuthBottomSheet.tsx with link + human-verified Supabase dashboard configuration.
</objective>

<execution_context>
@/Users/jaimeberdejosanchez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaimeberdejosanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-demo-auth-simplification/13-RESEARCH.md
@.planning/phases/13-demo-auth-simplification/13-01-SUMMARY.md

@frontend/components/AuthBottomSheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Create account" link to AuthBottomSheet</name>
  <files>frontend/components/AuthBottomSheet.tsx</files>
  <action>
    In `frontend/components/AuthBottomSheet.tsx`, add a "Create account" link below the Google sign-in button (after line 152, before the closing `</div>` of the sheet panel).

    Add a plain anchor or button element that navigates to `/auth/login`:

    ```tsx
    <a
      href="/auth/login"
      style={{
        color: 'rgba(255,255,255,0.5)',
        fontSize: '0.8rem',
        fontFamily: 'system-ui, -apple-system, sans-serif',
        textDecoration: 'underline',
        cursor: 'pointer',
        marginTop: '-8px',
      }}
    >
      Create account
    </a>
    ```

    Use a plain `<a href="/auth/login">` rather than Next.js `<Link>` — the login page is a full-page navigation and AuthBottomSheet does not import `next/link`. This avoids adding a new import and keeps the pattern consistent with the existing `window.location.href` approach in the same component.

    Do NOT embed a form, add new state, import signUp, or change the Google button in any way. The only change is adding this one anchor element below the Google button inside the sheet panel div.

    The Register tab on /auth/login defaults to the 'register' tab (set in Plan 01, Task 2) so navigating to /auth/login will land the user directly on the registration form.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/AutoNews_AI/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Trigger an AuthBottomSheet (tap like/bookmark/comment as a guest). Confirm "Create account" link appears below the Google button. Tap it — confirm navigation to /auth/login with Register tab active.</manual>
  </verify>
  <done>
    AuthBottomSheet renders "Create account" link below Google button. Tapping navigates to /auth/login. TypeScript compiles with no errors.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Disable email confirmation in Supabase dashboard</name>
  <action>
    Log in to the Supabase dashboard and disable the "Confirm email" setting so newly registered demo users are signed in immediately after registering (no email verification step required).

    Steps:
    1. Open https://supabase.com/dashboard
    2. Select the AutoNews AI project
    3. Navigate to: Authentication > Providers > Email
    4. Find the "Confirm email" toggle — turn it OFF (disabled)
    5. Click Save

    Why this is needed: The signUp Server Action (modified in Plan 01) now calls redirect('/') on success. With "Confirm email" enabled, Supabase returns a session-less response and the user lands on '/' as a guest — they appear not to be signed in even though they registered. Disabling confirmation means Supabase creates the session immediately on signUp, and the redirect to '/' lands them as a fully authenticated user.

    Note: This does NOT affect existing Google OAuth users — they are unaffected by this setting.
    Note: This is a project-level setting. If you re-enable it later, update the signUp action to return { message } instead of redirect('/').
  </action>
  <resume-signal>Type "done" once the Supabase "Confirm email" toggle is set to OFF and saved.</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete and the Supabase dashboard step is done:

1. `cd /Users/jaimeberdejosanchez/projects/AutoNews_AI/frontend && npx tsc --noEmit` — zero TypeScript errors
2. Full demo registration flow test:
   a. Visit the app as a guest
   b. Tap any social button (like/bookmark/comment) to trigger AuthBottomSheet
   c. Tap "Create account" — confirm navigation to /auth/login on Register tab
   d. Fill in email + password + confirm password — submit
   e. Confirm redirect to '/' and user appears signed in (profile icon visible, not guest state)
3. Sign-in tab still works: tap "Sign in" tab, click "Continue with Google" — OAuth flow completes normally
</verification>

<success_criteria>
- AuthBottomSheet has a "Create account" plain anchor link below the Google button pointing to /auth/login
- Google button in AuthBottomSheet unchanged
- Supabase "Confirm email" toggle is OFF (confirmed by human)
- Full demo flow: register via email → land on '/' as authenticated user
- TypeScript build passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-demo-auth-simplification/13-02-SUMMARY.md` following the summary template at @/Users/jaimeberdejosanchez/.claude/get-shit-done/templates/summary.md
</output>
