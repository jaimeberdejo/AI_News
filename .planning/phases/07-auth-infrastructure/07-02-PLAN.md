---
phase: 07-auth-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - frontend/supabase/migrations/20260323000001_add_profiles_and_auth.sql
  - frontend/app/auth/callback/route.ts
  - frontend/app/auth/confirm/route.ts
  - frontend/app/auth/actions.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "A new user can sign up with email and password and land on the feed signed in"
    - "An existing user can sign in with email/password"
    - "A user can request a password reset email and set a new password via the link"
    - "A user can sign in with Google OAuth and land back on the feed with an active session"
    - "The profiles row is auto-created on first sign-in with no application code required"
    - "Signing out clears the session and redirects to /"
  artifacts:
    - path: "frontend/supabase/migrations/20260323000001_add_profiles_and_auth.sql"
      provides: "profiles table, RLS policies, handle_new_user trigger"
      contains: "CREATE TABLE public.profiles"
    - path: "frontend/app/auth/callback/route.ts"
      provides: "GET handler — exchanges OAuth code for session (Google redirect lands here)"
      exports: ["GET"]
    - path: "frontend/app/auth/confirm/route.ts"
      provides: "GET handler — verifies email OTP tokens for signup confirm + password reset"
      exports: ["GET"]
    - path: "frontend/app/auth/actions.ts"
      provides: "Server Actions for signUp, signIn, signOut, resetPassword, updatePassword"
      exports: ["signUp", "signIn", "signOut", "resetPassword", "updatePassword"]
  key_links:
    - from: "frontend/app/auth/callback/route.ts"
      to: "frontend/lib/supabase/server.ts"
      via: "createClient() + exchangeCodeForSession(code)"
      pattern: "exchangeCodeForSession"
    - from: "frontend/app/auth/confirm/route.ts"
      to: "frontend/lib/supabase/server.ts"
      via: "createClient() + verifyOtp({ type, token_hash })"
      pattern: "verifyOtp"
    - from: "frontend/app/auth/actions.ts"
      to: "frontend/lib/supabase/server.ts"
      via: "createClient() for every Server Action"
      pattern: "createClient"
    - from: "Supabase auth.users INSERT"
      to: "public.profiles"
      via: "on_auth_user_created trigger"
      pattern: "handle_new_user"
---

<objective>
Create the database schema for user profiles (with auto-create trigger + RLS), the two auth redirect route handlers (OAuth callback and email confirm), and the Server Actions for all auth operations.

Purpose: This is the auth surface itself — the SQL migration sets up the data model, the route handlers receive Supabase redirects, and the Server Actions provide the API that the Phase 8 UI will call.

Output: SQL migration ready to run against Supabase; four route/action files implementing every AUTH-01 through AUTH-04 flow.
</objective>

<execution_context>
@/Users/jaimeberdejosanchez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaimeberdejosanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-auth-infrastructure/07-RESEARCH.md
@.planning/phases/07-auth-infrastructure/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create profiles migration SQL</name>
  <files>frontend/supabase/migrations/20260323000001_add_profiles_and_auth.sql</files>
  <action>
Create the directory frontend/supabase/migrations/ and write the migration file.

CRITICAL: The trigger function must be kept minimal (only insert id, display_name, avatar_url) and must include ON CONFLICT DO NOTHING to prevent the trigger from crashing signups if called more than once. A crash in handle_new_user() rolls back the entire signup transaction — never skip the safety valve.

--- frontend/supabase/migrations/20260323000001_add_profiles_and_auth.sql ---

-- ============================================================
-- profiles table
-- Mirrors auth.users. Auto-populated by trigger on signup.
-- ============================================================
CREATE TABLE public.profiles (
  id           uuid NOT NULL REFERENCES auth.users ON DELETE CASCADE,
  display_name text,
  avatar_url   text,
  created_at   timestamptz DEFAULT now(),
  updated_at   timestamptz DEFAULT now(),
  PRIMARY KEY (id)
);

ALTER TABLE public.profiles ENABLE ROW LEVEL SECURITY;

-- All users (including anonymous) can read profiles (needed for comment display in Phase 10)
CREATE POLICY "profiles are viewable by everyone"
  ON public.profiles FOR SELECT
  TO authenticated, anon
  USING (true);

-- Users can only update their own profile row
CREATE POLICY "users can update own profile"
  ON public.profiles FOR UPDATE
  TO authenticated
  USING (auth.uid() = id)
  WITH CHECK (auth.uid() = id);

-- ============================================================
-- Auto-create trigger: fires after every new auth.users insert
-- Creates the corresponding profiles row automatically.
-- SECURITY DEFINER + empty search_path prevents search-path injection.
-- ============================================================
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS trigger
LANGUAGE plpgsql
SECURITY DEFINER SET search_path = ''
AS $$
BEGIN
  INSERT INTO public.profiles (id, display_name, avatar_url)
  VALUES (
    new.id,
    new.raw_user_meta_data ->> 'full_name',
    new.raw_user_meta_data ->> 'avatar_url'
  )
  ON CONFLICT (id) DO NOTHING;  -- safety valve: prevents crash on duplicate
  RETURN new;
END;
$$;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE PROCEDURE public.handle_new_user();

Run this migration against the Supabase project using the Supabase CLI or by pasting it into the Supabase Dashboard SQL Editor (Database → SQL Editor → New query).

To run via CLI (if supabase CLI is authenticated):
  supabase db push

Or paste directly in Supabase Dashboard → Database → SQL Editor.
  </action>
  <verify>
    <automated>test -f /Users/jaimeberdejosanchez/projects/AutoNews_AI/frontend/supabase/migrations/20260323000001_add_profiles_and_auth.sql && echo "migration file exists" || echo "MISSING"</automated>
    <manual>After running the migration in Supabase Dashboard or via CLI: go to Supabase Dashboard → Table Editor and confirm the `profiles` table exists with columns id, display_name, avatar_url, created_at, updated_at. Check Database → Functions for `handle_new_user`. Check Database → Triggers for `on_auth_user_created` on auth.users.</manual>
  </verify>
  <done>Migration file exists locally. When applied to Supabase, the profiles table, two RLS policies, trigger function, and trigger are all present.</done>
</task>

<task type="auto">
  <name>Task 2: Create auth route handlers (OAuth callback + email confirm)</name>
  <files>
    frontend/app/auth/callback/route.ts,
    frontend/app/auth/confirm/route.ts
  </files>
  <action>
Create two GET route handlers. These routes receive redirects from Supabase after OAuth and email verification flows. They must never be called from client code directly.

--- frontend/app/auth/callback/route.ts ---
Handles the Google OAuth redirect. Supabase sends the user here with a `code` param after successful Google sign-in. exchangeCodeForSession() exchanges the one-time code for a real session (PKCE).

import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'

export async function GET(request: Request) {
  const { searchParams, origin } = new URL(request.url)
  const code = searchParams.get('code')
  const next = searchParams.get('next') ?? '/'

  if (code) {
    const supabase = await createClient()
    const { error } = await supabase.auth.exchangeCodeForSession(code)
    if (!error) {
      return NextResponse.redirect(`${origin}${next}`)
    }
  }

  // Exchange failed — redirect to error page (Phase 8 will build this page)
  return NextResponse.redirect(`${origin}/auth/auth-error`)
}

--- frontend/app/auth/confirm/route.ts ---
Handles email link clicks for both signup confirmation and password reset.
- Signup: Supabase sends token_hash + type=signup
- Password reset: Supabase sends token_hash + type=recovery + next=/auth/update-password

import { type EmailOtpType } from '@supabase/supabase-js'
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'

export async function GET(request: NextRequest) {
  const { searchParams } = new URL(request.url)
  const token_hash = searchParams.get('token_hash')
  const type = searchParams.get('type') as EmailOtpType | null
  const next = searchParams.get('next') ?? '/'

  if (token_hash && type) {
    const supabase = await createClient()
    const { error } = await supabase.auth.verifyOtp({ type, token_hash })
    if (!error) {
      return NextResponse.redirect(new URL(next, request.url))
    }
  }

  return NextResponse.redirect(new URL('/auth/auth-error', request.url))
}
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/AutoNews_AI/frontend && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20 || echo "no type errors"</automated>
  </verify>
  <done>Both route files exist. TypeScript compiles without errors. GET is the only export from each file.</done>
</task>

<task type="auto">
  <name>Task 3: Create auth Server Actions</name>
  <files>frontend/app/auth/actions.ts</files>
  <action>
Create frontend/app/auth/actions.ts with all five Server Actions. These will be imported by Phase 8 UI components (login forms, reset forms). All actions use createClient() from lib/supabase/server.ts — never the anon singleton from lib/supabase.ts.

CRITICAL: For Google OAuth (signInWithOAuth), return the URL to the caller — do NOT redirect server-side. The client must use window.location.href = url, not window.open(). This is required for iOS PWA standalone mode compatibility. The Server Action returns { url: string } and the Client Component does the redirect. (Phase 8 will wire this up.)

--- frontend/app/auth/actions.ts ---
'use server'

import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
import { headers } from 'next/headers'

// AUTH-01: Sign up with email and password
// Sends a confirmation email; user lands on / after clicking the link (via /auth/confirm)
export async function signUp(formData: FormData) {
  const supabase = await createClient()
  const headersList = await headers()
  const origin = headersList.get('origin') ?? process.env.NEXT_PUBLIC_APP_URL ?? ''

  const { error } = await supabase.auth.signUp({
    email: formData.get('email') as string,
    password: formData.get('password') as string,
    options: {
      emailRedirectTo: `${origin}/auth/confirm`,
    },
  })

  if (error) throw new Error(error.message)

  // Return instead of redirect so the UI can show "check your email" message
  return { message: 'Check your email to confirm your account.' }
}

// AUTH-02: Sign in with email and password
export async function signIn(formData: FormData) {
  const supabase = await createClient()

  const { error } = await supabase.auth.signInWithPassword({
    email: formData.get('email') as string,
    password: formData.get('password') as string,
  })

  if (error) throw new Error(error.message)
  redirect('/')
}

// AUTH-04: Initiate Google OAuth sign-in
// Returns the OAuth redirect URL — the Client Component must do window.location.href = url
// NEVER use window.open() — it is broken in iOS PWA standalone mode
export async function signInWithGoogle(): Promise<{ url: string }> {
  const supabase = await createClient()
  const headersList = await headers()
  const origin = headersList.get('origin') ?? process.env.NEXT_PUBLIC_APP_URL ?? ''

  const { data, error } = await supabase.auth.signInWithOAuth({
    provider: 'google',
    options: {
      redirectTo: `${origin}/auth/callback`,
    },
  })

  if (error || !data.url) throw new Error(error?.message ?? 'OAuth URL missing')
  return { url: data.url }
}

// AUTH-03: Request a password reset email
export async function resetPassword(formData: FormData) {
  const supabase = await createClient()
  const headersList = await headers()
  const origin = headersList.get('origin') ?? process.env.NEXT_PUBLIC_APP_URL ?? ''

  const { error } = await supabase.auth.resetPasswordForEmail(
    formData.get('email') as string,
    {
      redirectTo: `${origin}/auth/confirm?next=/auth/update-password`,
    }
  )

  if (error) throw new Error(error.message)
  return { message: 'Check your email for the reset link.' }
}

// AUTH-03 (continued): Update password after clicking reset link
// Called from the /auth/update-password page (Phase 8)
export async function updatePassword(formData: FormData) {
  const supabase = await createClient()

  const { error } = await supabase.auth.updateUser({
    password: formData.get('password') as string,
  })

  if (error) throw new Error(error.message)
  redirect('/')
}

// Sign out
export async function signOut() {
  const supabase = await createClient()
  await supabase.auth.signOut()
  redirect('/')
}
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/AutoNews_AI/frontend && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20 || echo "no type errors"</automated>
  </verify>
  <done>app/auth/actions.ts exports signUp, signIn, signInWithGoogle, resetPassword, updatePassword, signOut. TypeScript compiles without errors. The file is marked 'use server' at the top.</done>
</task>

</tasks>

<verification>
TypeScript compiles clean across the entire project: `npx tsc --noEmit`

Migration SQL file exists and contains `CREATE TABLE public.profiles`, `handle_new_user` function, and `on_auth_user_created` trigger.

All exports are present:
- app/auth/callback/route.ts: exports GET
- app/auth/confirm/route.ts: exports GET
- app/auth/actions.ts: exports signUp, signIn, signInWithGoogle, resetPassword, updatePassword, signOut

The migration has been applied to the Supabase project (profiles table visible in dashboard).
</verification>

<success_criteria>
- Migration file exists at frontend/supabase/migrations/20260323000001_add_profiles_and_auth.sql
- Migration applied to Supabase: profiles table + RLS policies + trigger visible in dashboard
- app/auth/callback/route.ts handles GET with exchangeCodeForSession
- app/auth/confirm/route.ts handles GET with verifyOtp
- app/auth/actions.ts has all six Server Actions marked 'use server'
- signInWithGoogle returns { url } — does NOT call redirect() or window.location.href server-side
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-auth-infrastructure/07-02-SUMMARY.md`
</output>
